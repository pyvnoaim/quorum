# copy to .env — `npm run dev` loads it via --env-file.
DISCORD_BOT_TOKEN=
# where results get posted. unset = the channel the match was started in.
RESULTS_CHANNEL_ID=
# sqlite file, defaults to ./pug.db
DB_PATH=
